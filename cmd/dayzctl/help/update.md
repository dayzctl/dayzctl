Check for server updates via SteamCMD, stop running instances, perform the update, sync mods, and restart previously running instances.

This command requires SteamCMD to be configured and may prompt for credentials or use the configured Steam user.

Usage:
  dayzctl update

Examples:
  dayzctl update
