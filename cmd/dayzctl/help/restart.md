Restart systemd units for one or more instances.

Equivalent to stop then start; use 'all' to restart every enabled instance.

Usage:
  dayzctl restart <instance>
  dayzctl restart all

Examples:
  dayzctl restart myserver
  dayzctl restart all
