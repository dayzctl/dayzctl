Perform a basic validation of the loaded configuration and print summary information about instances, paths, and Steam settings.

Usage:
  dayzctl validate-config

Examples:
  dayzctl validate-config
